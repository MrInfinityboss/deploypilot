"use client";
import { useEffect,useState } from "react"; import { useParams,useRouter } from "next/navigation"; import { createBrowserClient } from "@supabase/ssr"; import { apiRequest } from "../../../../lib/api"; import { Badge,Card,PageHeader,formatDate } from "../../ui";
type Stage={name:string;status:string;startedAt?:string;endedAt?:string};type Detail={id:string;status:string;commitSha:string;createdAt:string;startedAt:string|null;endedAt:string|null;repository:{fullName:string};environment:{name:string;url:string|null}|null;stages:Stage[];config:{branchRule:string;version:number}};type Log={sequence:number;stage:string;level:string;message:string;createdAt?:string};
const stageNames=['dependencies','tests','docker-build','health-check','deploy'];
export default function DetailPage(){const {deploymentId}=useParams<{deploymentId:string}>();const router=useRouter();const [d,setD]=useState<Detail|null>(null);const [logs,setLogs]=useState<Log[]>([]);const [connected,setConnected]=useState(false);const [msg,setMsg]=useState('Loading deployment…');
 const load=async()=>{try{const x=await apiRequest<Detail>(`/v1/deployments/${deploymentId}`);setD(x);const l=await apiRequest<{logs:Log[]}>(`/v1/deployments/${deploymentId}/logs`);setLogs(l.logs)}catch(e){setMsg(e instanceof Error?e.message:'Unable to load deployment.')}};
 useEffect(()=>{let source:EventSource|undefined;let retry:number|undefined;let closed=false;void load();const connect=async()=>{const supabase=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const {data}=await supabase.auth.getSession();if(!data.session)return setMsg('Sign in to view live deployment events.');const url=`${process.env.NEXT_PUBLIC_API_URL}/v1/deployments/${deploymentId}/events?access_token=${encodeURIComponent(data.session.access_token)}`;source=new EventSource(url);source.onopen=()=>{setConnected(true);setMsg('Live event stream connected.');};source.onerror=()=>{setConnected(false);source?.close();if(!closed)retry=window.setTimeout(connect,2000);};source.addEventListener('deployment.status',e=>{const payload=JSON.parse((e as MessageEvent).data) as {status:string};setD(previous=>previous?{...previous,status:payload.status}:previous);if(!['QUEUED','RUNNING'].includes(payload.status))void load();});source.addEventListener('stage.updated',e=>{const payload=JSON.parse((e as MessageEvent).data) as Stage;setD(previous=>previous?{...previous,stages:previous.stages.map(stage=>stage.name===payload.name?{...stage,...payload}:stage)}:previous);});source.addEventListener('log.appended',e=>{const payload=JSON.parse((e as MessageEvent).data) as Log;setLogs(previous=>previous.some(log=>log.sequence===payload.sequence)?previous:[...previous,payload].sort((a,b)=>a.sequence-b.sequence));});};void connect();return()=>{closed=true;if(retry)window.clearTimeout(retry);source?.close()};},[deploymentId]);
 const action=async(path:string)=>{try{await apiRequest(`/v1/deployments/${deploymentId}/${path}`,{method:'POST'});await load();if(path==='retry')router.refresh()}catch(e){setMsg(e instanceof Error?e.message:'Action failed.')}};if(!d)return <><PageHeader eyebrow="Operations / Deployment" title="Deployment detail"/><Card>{msg}</Card></>;return <><PageHeader eyebrow={`Operations / ${d.repository.fullName}`} title={d.commitSha.slice(0,12)} description={`Created ${formatDate(d.createdAt)} · ${d.environment?.name??'No environment'} · profile ${d.config.branchRule} v${d.config.version}`} action={<div style={{display:'flex',gap:8}}>{['QUEUED','RUNNING'].includes(d.status)&&<button className="dp-btn dp-btn-danger" onClick={()=>action('cancel')}>Cancel</button>}{['FAILED','TIMED_OUT'].includes(d.status)&&<><button className="dp-btn" onClick={()=>action('retry')}>↻ Retry</button><button className="dp-btn dp-btn-primary" onClick={()=>action('diagnose')}>✦ Diagnose</button></>}</div>}/><Card style={{marginBottom:16}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:22}}><div><div className="dp-kicker">Deployment status</div><div style={{fontSize:20,fontWeight:800,marginTop:8}}>{d.status}</div></div><div style={{display:'flex',gap:10,alignItems:'center'}}><span style={{fontSize:11,color:connected?'var(--green)':'var(--muted)'}}>● {connected?'LIVE':'RECONNECTING'}</span><Badge status={d.status}/></div></div><div style={{display:'grid',gridTemplateColumns:'repeat(5,1fr)',gap:4}}>{stageNames.map(name=>{const s=d.stages.find(stage=>stage.name===name);return <div key={name}><div style={{height:7,borderRadius:8,background:s?.status==='SUCCEEDED'?'var(--green)':s?.status==='RUNNING'?'var(--yellow)':s?.status==='FAILED'?'var(--red)':'#303a4b'}}/><div style={{fontSize:10,color:'var(--muted)',marginTop:8}}>{name}</div></div>})}</div></Card><div className="dp-grid-2" style={{display:'grid',gridTemplateColumns:'1.25fr .75fr',gap:16}}><Card><div style={{display:'flex',justifyContent:'space-between',marginBottom:13}}><h2 style={{fontSize:15,margin:0}}>Live logs</h2><span className="dp-mono" style={{fontSize:11,color:'var(--muted)'}}>{logs.length} lines</span></div><div style={{background:'#080a0e',borderRadius:9,padding:16,minHeight:350,maxHeight:520,overflow:'auto'}}>{logs.length?logs.map(l=><div className="dp-mono" key={l.sequence} style={{fontSize:11,lineHeight:1.8,color:l.level==='error'?'var(--red)':'#b6c1d1'}}><span style={{color:'#657187',marginRight:12}}>{String(l.sequence).padStart(4,'0')}</span><span style={{color:'var(--purple)',marginRight:9}}>{l.stage}</span>{l.message}</div>):<div style={{color:'var(--muted)',paddingTop:120,textAlign:'center'}}>Waiting for worker events…</div>}</div></Card><Card><h2 style={{fontSize:15,margin:'0 0 18px'}}>Run details</h2><div style={{display:'grid',gap:15}}>{[['Repository',d.repository.fullName],['Commit',d.commitSha],['Environment',d.environment?.name??'—'],['Started',formatDate(d.startedAt)],['Finished',formatDate(d.endedAt)]].map(([k,v])=><div key={k}><div className="dp-kicker">{k}</div><div className="dp-mono" style={{fontSize:11,marginTop:5,wordBreak:'break-all'}}>{v}</div></div>)}</div></Card></div><div style={{color:'var(--muted)',fontSize:12,marginTop:14}}>{msg}</div></>}
